# SiteLens 5.0 ML Pretraining Task Plan（开发文档）

> 分支：`feature/ml-5.0-pretraining`。前置文档：docs/审计报告-5.0-ML预训练.md
> （下称"审计报告"，本文件所有"见审计报告 §N"均指该文）。
> 本计划完全基于源码审计结论，不含通用 ML 模板项。执行顺序：Phase 0 → 13，
> 每个 Phase 完成后独立 commit（提交信息前缀 `ml:`）。
>
> **修订 v2（2026-09-14）**：吸收十二点设计修正——T1 标签三分语义（executed/
> verified 区分）、历史特征严格时间截断、T2 收窄为 candidate-finding ranking、
> L3 降格为 sanity 任务、host/target split 等价性、replay 降为可选增强、
> score/probability/confidence 分离、数据量约束、固定执行顺序与 P5 数据
> gate。本计划不含任何 LLM/RAG/Agent/Multi-Agent 内容。

## 0. 边界与禁令（全程有效）

- 5.0 核心是 Machine Learning（统计学习/排序/树模型起步）。**禁止**：
  LLM、RAG、Chat、Agent、Multi-Agent、Prompt Engineering、AI Chat UI。
- 本阶段**禁止**：在线自动攻击、自动化生产攻击、修改 3.0 扫描逻辑、
  修改 4.0 主线代码、把 ML 接入生产扫描流程、伪造训练数据/指标
  （Accuracy/Precision/Recall/F1/AUC 一律只来自真实运行）、改测试集凑指标、
  数据泄漏、用未来结果做特征。
- **负样本纪律（v2 新增）**：没有 execution 证据（该验证面确实执行过且未
  命中）不得生成 negative label；"没有 verified 记录"不等于负样本；
  不得为了完成 TODO 或提升指标人为构造正负样本。
- 提前实现 4.0 尚不存在的攻击链/CWE 关系/权限变化字段同样禁止——接口只做
  schema 定义（Phase 11）。
- 生成训练数据的主动扫描只在**自有靶场/自有资产**上做（农场
  `D:\fengqiao\ranges-lab`），exploit 开闸仅限靶场配置，不改默认值。
- 与现有结构冲突时以现有项目规范为准：数据目录 git 忽略策略、
  `data/state/` 只读、历史库格式冻结（审计报告 §16）。
- 语言：ML 流水线用 Python 3.13（本机既有），纯本地运行；
  依赖进 `ml/requirements.txt`（numpy/pandas/scikit-learn 起步，
  禁深度学习框架进第一阶段）。

## 0.5 第一阶段核心成果（v2 新增，最高优先级声明）

**第一阶段的核心成果不是"训练出高准确率模型"。**

第一阶段核心成果是一条**可重复、可追溯、无明显数据泄漏的 ML 数据生产线**：

```
真实历史数据 → Dataset → Feature → Label → Leakage Check → Split
            → Baseline → ML → Evaluation → Model Manifest
```

当前真实数据规模（审计报告 §7 实测，所有设计以此为前提）：
**119 scans / 10 hosts / 456 verified 行 / 1,135 intel 行**；exploit 判定
样本 0；可靠执行日志 0。在这个量级上，复杂模型和高指标既不可靠也不诚实。
模型选择固定为 Logistic Regression / Random Forest / Gradient Boosting
三族，基线固定为 Severity / CVSS / 现有 rule-based priority 三条，
全程不得为提高指标制造 synthetic 正负样本。

## 1. ML 数据目录规范（进仓的是 schema/manifest/报告，数据本体 git 忽略）

```
data/ml/                  # 整体加入 .gitignore（新增一行，唯一允许的仓级改动）
  raw/                    # history.json 只读快照 + 来源指针（raw_manifest.json）
  dataset/                # 清洗后的标准化数据集（parquet/jsonl）+ DATASET_MANIFEST
  features/               # 特征表 + FEATURE_MANIFEST
  labels/                 # 标签表 + LABEL_MANIFEST
  models/                 # 模型产物（joblib）+ MODEL_MANIFEST
  experiments/            # 每次训练一个目录：config+manifest+metrics.json
  metrics/                # 汇总指标（真实运行产出，append-only）
```

版本化对象与追溯字段（每个 manifest 必含）：
`kind(dataset|feature|label|model|experiment)`、`version`（语义化递增）、
`created_at`、`git_rev`、`inputs[]`（文件路径+sha256+行数/条数）、
`params`、`range`（scan_id 区间/host 集合/时间区间）、`producer`
（生成脚本+版本）。一次训练可由 experiment manifest 反查全部上游版本。

## 2. Dataset Schema（字段全部映射真实结构，无虚构字段）

粒度四层 + 一张静态特征表，主键与来源：

**D1 站点扫描表（scan 级，主键 scan_id）**
| 字段 | 类型 | 来源（真实结构） |
|---|---|---|
| sample_id | string | `{scan_id}` |
| target/host/ip | string | ScanRecord.URL/Host, Result.IP |
| scanned_at | datetime | ScanSummary.ScannedAt（存原始串+解析双份；**即该样本的 prediction_time**） |
| era | enum(py|go) | scan_id≤47 → py（审计报告 §9.4 实证） |
| options_known | json | ScanRecord.Options（原样，含缺失键标记） |
| duration_s/response_time_ms/status/error | num | Result 同名字段 |
| page_count | int | len(Result.Pages) |
| sec_score/sec_grade/sec_items[8] | num/enum/bool[] | Result.Security（security.go 8 头） |
| n_tech/n_vuln/n_verified | int | ScanSummary 计数 |
| data_origin | enum(history|archive|premigrate|matrix) | 文件来源指针 |

**D2 技术表（tech 级，主键 scan_id+tech_name）**：name、categories[]、
confidence、version、has_version、evidence_count、evidence_channel_types[]
（从 evidence 字符串前缀解析：header/meta/正则/src/内联JS/icon_hash/cookie/
关键词）→ 来源 engine.Tech（result.go:14-21）。

**D3 情报表（intel 级，主键 scan_id+cve+tech；同时是 T2 的 candidate
finding 来源之一）**：cve、tech、version、verdict、affected、severity_raw、
severity_norm（归一后）、cvss_score、cvss_sev、kev、n_templates_attached、
src、published_days（联 NVD.pub）、title/desc 文本统计 → 来源
intel.Finding（intel.go:66-86）。

**D4 验证表（verified 级，主键 scan_id+row_idx；同时是 T2 的 verification
target 来源）**：src、check、severity、url_path、param、has_payload、
confirmed、has_evidence_chain（request/replay/response/signals 五键齐）、
resp_status、resp_size、signals_count、impact（可空，显式 missing）、
evidence 文本统计 → 来源 verified map（审计报告 §8）。

**D4 v2 新增字段——observation/execution 状态**（修正点 1 的落地）：

| 字段 | 取值 | 语义 |
|---|---|---|
| execution_status | `executed` / `not_executed` / `unknown` | 该验证面在该次扫描中是否真正执行 |
| execution_basis | string | 判定依据说明（如 `options.checks 档位推断` / `无执行日志`） |

历史事实与推断规则（诚实口径）：
- verified 行本身 = executed ∧ 命中（命中才落库），天然 positive。
- 内置 check 的"被选中"可由落库的 `options.checks`（none/core/all）对
  37 条内置规则表推断，但**单次请求成败无记录**——推断不出可靠
  executed，一律 `unknown`。
- Nuclei 子集按 LRU 运行期选取（engine.go:681-719），DAST 探测集依赖
  爬取产物，执行集**不可复原** → `unknown`。
- 结论：**现有历史无法可靠产生任何 executed ∧ 未命中 的负样本**。
  Dataset 统计报告必须给出 execution_status 分布、unknown 占比、
  以及"可组合验证面（scans×适用 check/template）中 positive 456 /
  其余全部 unknown"的量级说明。**不得把 unknown 记为 negative。**

**D5 静态联接表（字典级，版本=字典文件 sha256）**：
CVE→(cvss 向量分解 AV/AC/PR/UI/S/C/I/A, kev, ransomware, pub, mod,
cpe products)；tech→(cats, conf, 有无 curated ranges)；template→
(path, tags, sev, proto, convertible, cves)；service_fp 行。
来源：data/nvd_cves.json.gz / intel_dump.json.gz / tpl_intel.json.gz /
affected_ranges.json / data/state/nuclei_index.json / data/go/technologies.json。

**隔离与追踪要求**：每行带 scan_id/host/scanned_at/data_origin；
split 元数据独立成文件；同一 host 任何情况下不得跨 split（见 §5 Phase 8
的 host/target 等价性规则）。

## 3. Feature 设计（全部标注 Prediction-Time 语义）

| 组 | 特征 | 语义 | 来源 |
|---|---|---|---|
| 资产/网络 | scheme、port、is_private_ip、host_tld、page_count、rt_ms、sec_* | P0 | D1 |
| 技术栈 | tech one-hot（115→频次截断）、cat 聚合 33 类、confidence 统计、version 覆盖率、共现 top 对 | P0 | D2 |
| 情报/CVE | cvss 分解 8 维、kev、ransomware、pub_days、src 类别、tpl_intel 覆盖、curated_range 有无、模板可用数 | P0（静态字典） | D3+D5 |
| 验证面 | check/template 元数据（lv、tags、sev、proto）、历史命中先验（**严格时间截断**，见下） | P0/P1 | D4+D5 |
| 证据强度 | confirmed、signals_count、resp_size、has_chain、evidence 长度/关键词 | P1 | D4 |
| 时序/时延 | timing 抽取数值（从 signals 正则提取 ms）、duration | P1 | D4/D1 |
| 认证态 | **缺**（结构化未落库）——特征位显式 missing | — | — |
| 攻击链/白盒 | **缺**——等待 4.0（接口见 Phase 11），Node/Edge/Path/Chain 特征已在接口草案预留 | — | — |

**历史先验特征时间截断（v2 硬规则，代码实现而非文档声明）**：

适用特征（穷举）：host 历史 verified 次数、host 历史漏洞（intel 行）次数、
CVE/check/template 历史命中次数、历史验证成功率、历史扫描结果聚合
（如该 host 上次扫描的安全评分）。

规则：对 prediction_time = T（样本自身的 scanned_at）的任何样本，
先验特征只允许聚合 `scanned_at < T` 的记录；**同一样本所属扫描自身、
以及 T 之后产生的全部记录禁入**。实现落点：
- `ml/features.py`：as-of 聚合器（按时间排序后前缀聚合，禁止全表 join）；
- `ml/leakage.py`：断言——随机抽取样本，重算其先验特征，逐一验证
  参与聚合的记录 scanned_at 均严格早于该样本 prediction_time；
  违例即 fail，评估不得开始。

禁止入特征：impact/impact_evidence、Extras["exploit"]、重放结果、
以及其他任何 F 语义（未来结果）字段——黑名单以断言形式在 Phase 5 实现。

## 4. Label 设计（可靠性逐条声明）

| ID | 标签 | 定义 | 可靠性 | 现状 |
|---|---|---|---|---|
| L1 | verified-hit | **三分**：executed ∧ 命中 = positive；executed ∧ 未命中 = negative；未执行/无法判定 = unknown（不训练、不进指标分母，仅统计） | positive 可靠；**negative 当前 0**（无执行日志，见 §2 D4） | positive 456 行可用 |
| L2 | template-verify-hit | (host, cve) 模板真命中 | 高 | 需受控靶场扫描生成（可选增强，不阻塞基础管线） |
| L3 | confirmed-verdict | 版本落受影响区间 | 高但近确定性规则（versioncmp）。**定位：sanity / pipeline validation 任务**——大量复用 version range / affected range / 情报匹配等既有规则，不是 5.0 核心智能能力，**不投入主要资源**，仅用于验证数据管线正确性 | 12 行 |
| L4 | regression-present | 回归重放仍在 | 高 | **可选数据增强**（Phase 5 选做），非基础 Dataset Pipeline 依赖；无 replay 数据时基础训练流程必须照常可运行 |
| L5 | exploit-proven | 利用级 proven/observed | 语义最优但**当前 0 样本，暂缓** | 靶场积累后启用 |
| L6 | 攻击链/权限/影响归因 | — | 不存在 | 等待 4.0 |

红线：Severity 不作为任何任务的 Label（只作基线/特征）；没有 execution
证据不得造 negative；不得为指标制造任何数据。

## 5. 任务定义（v2 收窄后的最终口径）

**T1 验证目标优先级（模板/check 执行结果预测）**
- 样本：目标 × 验证面（check/template）。
- 标签：L1 三分语义（§4）。**executed + verified/hit = positive；
  executed + not verified = negative；not executed = unknown，不得作为
  negative。**
- 数据现实：历史只记录命中（456 行 positive），无执行日志 → 可靠
  negative = 0，其余验证面组合全部 unknown。**T1 能否训练由 Phase 5
  数据 gate 按真实统计决定；不支持即延期，不得强行生成负样本。**

**T2 候选发现优先级排序（candidate-finding ranking，v2 收窄定义）**
- 输入：一次扫描产生的 candidate findings / verification targets
  （= D3 情报行 + D4 验证目标，附带该扫描的站点/技术栈/字典特征）。
- 输出：**ranking score + Top-K ranking**。
- 训练粒度：**scan × candidate finding**（备选 host × verification
  target）。不以 scan 或 host 整体作为训练粒度。
- 监督信号：候选是否进入 verified 的**观测正例**；未进入候选是
  unobserved 而非 negative，排序指标（P@K / nDCG@K）只定义在观测
  正例上，不把 unobserved 当负例计算。
- **对比义务**：T2 最终必须与 SiteLens 现行 severity 序 / CVSS 序 /
  rule-based priority 基线在同 split、同指标口径下对比（Phase 6/8）。

**T3** 站点级 sanity 回归（技术栈→verified 类别分布）：仅作管线自检。
**T4** 可利用性预测：暂缓（0 样本）。**T5** 验证策略推荐：暂缓。
**T6** 攻击链类：等待 4.0。

## 6. Phase 计划（v2 重排，编号对齐固定执行顺序）

### Phase 0：代码与数据审计（已完成——即审计报告本身）
- 目标：完成全库源码通读与真实数据画像。输入：仓库+data/。输出：审计报告。
- 验收：报告覆盖任务书全部审计项并被数据复核。风险：无。
- 依赖 4.0：否（4.0 状态已判定）。可执行：**已完成**。
- v2 追加修正：T1/T2/L3 定义、时间泄漏硬规则、split 等价性已回写审计报告。

### Phase 1：ML 数据基础设施
- 目标：建立 data/ml/ 目录规范、.gitignore 增补、`ml/` Python 包骨架
  （pyproject/requirements）、manifest 读写工具、配置模块（路径/版本常量）。
- 输入：本文件 §1。输出：可 `python -m ml.manifest` 生成/校验 manifest 的骨架包。
- 涉及目录：`ml/`（新增）、`data/ml/`（新增，git 忽略）、`.gitignore`（+1 行）。
- 新增文件：`ml/__init__.py`、`ml/paths.py`、`ml/manifest.py`、
  `ml/config.py`、`ml/requirements.txt`、`README.md`（包内说明）。
  修改文件：`.gitignore`。**不改任何 Go 文件。**
- 前置：无。数据依赖：无。验收：manifest 往返读写+sha256 校验单测过；
  git status 不出现 data/ml/。
- 风险：低。依赖 4.0：否。可执行：**是**。

### Phase 2：Dataset Pipeline
- 目标：`ml/dataset.py`：读取 history.json/archive/premigrate/靶场矩阵
  ScanRecord → D1-D5 五张表 → data/ml/dataset/ 落盘 + DATASET_MANIFEST。
- v2 新增：**T1 observation/execution 状态建模**——D4 增加
  execution_status/execution_basis 字段及 §2 推断规则（内置 check 档位
  推断、Nuclei/DAST 执行集不可复原→unknown）。
- 新增：`ml/readers.py`、`ml/dataset.py`、`ml/normalize.py`
  （severity 归一、era 标记、verified 键位规范化）、`ml/tests/test_dataset.py`。
- 数据依赖：Phase 1。前置：无。
- 验收：五表行数与审计报告 §7 数字一致（断言写进测试）；execution_status
  分布进入数据统计报告；重复键率、era 分层统计产出报告。
- 风险：premigrate 旧 schema 字段缺失（verdict 空 169 行）——规范化为
  显式 missing。依赖 4.0：否。可执行：**是**。

### Phase 3：Feature Pipeline
- 目标：`ml/features.py`：D1-D5 → §3 特征表；每特征带 prediction_time
  语义标注（P0/P1）；静态字典联接（NVD 向量分解/KEV/模板可用性）。
- v2 新增：**历史特征严格时间截断**——as-of 聚合器实现（仅聚合
  scanned_at < prediction_time 的记录），覆盖 §3 穷举的先验特征清单。
- 新增：`ml/features.py`、`ml/dict_join.py`、`ml/tests/test_features.py`
  （含"先验特征不含未来记录"用例）。
- 数据依赖：Phase 2。验收：特征表与 D 表行数对齐；黑名单字段断言不在
  表中；时间截断单测通过。
- 风险：evidence 中文文本特征保持统计级（长度/通道计数），不做 NLP 深加工。
  依赖 4.0：否（攻击链特征位留空）。可执行：**是**。

### Phase 4：Label Pipeline
- 目标：`ml/labels.py`：按 §4 生成 L1（三分语义）/L3（sanity 定位）/
  L5（显式 disabled）；每标签带可靠性说明与生成口径（POLE 文件）。
- v2 新增：**T1 unknown 样本隔离**——unknown 样本独立成表（unknown 层），
  不进入训练集与指标分母；其规模在统计报告中单列。
- 新增：`ml/labels.py`、`ml/tests/test_labels.py`。
- 数据依赖：Phase 2。验收：L1 positive 数=456（与实测一致）；
  negative 数=0 的现实如实写入 POLE；unknown 层独立可查。
- 风险：无（如实报告即达标）。依赖 4.0：否。可执行：**是**。

### Phase 5：Leakage / Data Quality（数据 gate）
- 目标：`ml/leakage.py`：host 隔离断言、时间切点工具、重复组
  （host+check+url）跨 split 检查、特征黑名单断言、**先验特征时间截断
  复核断言**（抽样重算）、farm/真实目标域标记；`ml/quality.py`：
  不平衡/稀疏/截断偏差/execution_status unknown 占比定量报告。
- v2 新增 1：**host/target split 等价性检查**——统计 target（扫描目标）
  与 host 的一一对应关系；当前数据中 target≡host（一扫一主机），二者
  不可分时**只保留 host split 并在评估报告说明原因**，不伪造两个独立
  实验；仅当数据中出现独立 target group（如同域多目标、IP/端口级资产）
  才分别统计。
- v2 新增 2：**replay 降为可选数据增强**——对 158 行带 replay 输入的行
  跑重放、结果存 data/ml/labels/ 属选做实验；**基础 Dataset Pipeline
  （Phase 2-4）不得依赖 replay 数据**，无 replay 时基础训练流程照常运行。
- v2 新增 3（数据 gate）：Phase 5 结束必须产出《真实数据统计报告》
  （execution_status 分布、positive/negative/unknown 计数、split 可分性、
  各候选任务的可训练性结论）。**T1/T2 的最终训练形式由此报告决定；
  数据不支持的任务必须延期，不得为了完成 TODO 人为构造数据。**
- 新增：`ml/leakage.py`、`ml/quality.py`、`ml/tests/test_leakage.py`。
- 修改：无。验收：全部断言可运行且通过；统计报告落 data/ml/metrics/。
- 风险：靶场扫描耗时（选做项用既有 matrix_par.sh，速率按硬件上限 2/3）。
  依赖 4.0：否。可执行：**是**。

### Phase 6：Baseline
- 目标：`ml/baselines.py` 规则基线：Severity 序（复刻 intel.go:88-90
  语义）、CVSS 降序、KEV 优先、现行 rule-based priority（nuclei.Select
  tags+相关度选模板的可计算近似）。**T2 的基线对比必须与 T2 同粒度
  （scan × candidate finding）、同指标口径。**
- 数据依赖：Phase 5 gate 通过的任务。验收：基线指标真实运行产出并入库。
  依赖 4.0：否。可执行：**是**（受 gate 影响的是后续训练，不是基线）。

### Phase 7：ML Training
- 目标：gate 放行的任务上训练 LR / RF / GBDT（scikit-learn）。
- v2 明确：T2 按 §5 定义实现 candidate-finding ranking（输出 ranking
  score + Top-K，粒度 scan×candidate finding）；T1 仅当 gate 放行
  （存在可靠 negative 或明确的正例排序口径）才训练。
- 新增：`ml/models.py`、`ml/train.py`、`ml/tests/test_models.py`。
- 数据依赖：Phase 5 gate + Phase 6。验收：模型只在 train split 拟合；
  指标对照基线；无任何手填数字；被 gate 延期的任务在文档与 TODO 中
  标注延期原因。
- 风险：小样本——验收标准为"流程正确+指标诚实"，不设"必须超基线"。
  依赖 4.0：否。可执行：**是**（范围由 gate 定）。

### Phase 8：Evaluation
- 目标：指标集——分类 P/R/F1、ROC-AUC、PR-AUC；排序 P@K、nDCG@K、
  MRR（T2 指标只定义在观测正例上）；split 口径：time split + host split，
  **target split 仅当 Phase 5 等价性检查确认可分时才单独统计**，否则
  只保留 host split 并在报告说明；泄漏检查作为评估前置门（不过不开跑）。
- 新增：`ml/evaluate.py`。依赖 4.0：否。可执行：**是**。

### Phase 9：Explainability / Calibration
- 目标：每条预测统一输出契约：**score / probability / confidence /
  top_features / prediction_reason**，三者严格分离：
  - `score`：模型原始打分（排序/阈值判断用），所有模型必给；
  - `probability`：仅当模型输出概率且**经过 Phase 9 校准验证可靠**
    （校准曲线/校准误差达标）才填，否则为 `null`；
  - `confidence`：第一阶段一律 `null`（保留字段）；
  - **未经 calibration 的模型输出不得称为"置信度"**。
- `top_features`（≤5 特征贡献）与 `prediction_reason`（引用触发特征值的
  中文短句，无 emoji）由树重要性/线性系数路径生成。
- 新增：`ml/explain.py`、`ml/calibrate.py`、`ml/tests/test_explain.py`。
- 依赖 4.0：否。可执行：**是**。

### Phase 10：Model Versioning
- 目标：MODEL_MANIFEST 与 experiments 索引（data/ml/metrics/summary.jsonl
  append-only）；`ml/registry.py` 提供"按版本加载模型+追溯上游"。
- 验收：任一模型可反查 dataset/feature/label 版本与 git rev。可执行：**是**。

### Phase 11：4.0 → 5.0 数据接口（仅 schema，不实现适配器，不改 4.0）
- 目标：把审计报告 §19 的投影 schema 固化为 `ml/interfaces/v40.py`
  （dataclass + JSON Schema + 显式 missing 语义）+《等待 4.0 数据清单》。
  **不提前实现 4.0 尚不存在的攻击链/CWE 关系/权限变化字段。**
- 依赖 4.0：接口定义不需要；数据接入需要。可执行：**是**（仅 schema）。

### Phase 12：Offline Prediction
- 目标：`ml/predict.py`：对指定 ScanRecord JSON 离线产出 gate 放行任务的
  ranking score + Top-K + 解释（零网络请求、不接生产扫描）。
- 依赖：Phase 7/9/10。可执行：**是**（属离线工具）。

### Phase 13：5.0 ML Pretraining 完成
- 目标：汇总报告（数据/特征/标签/基线/模型/指标/局限/等待 4.0 清单/
  gate 延期任务及原因）+ 端到端复现演练（删 data/ml/ 重建一次成功）。
  产出文档 `docs/开发文档-5.0-ML预训练-完成报告.md`。
  可执行：**是**（前置 Phase 1-12）。

## 7. 执行顺序（v2 固定）与任务依赖

**固定主线（不得跳步）**：

```
P1 数据基础设施(Phase 1) → P2 Dataset(2) → P3 Feature(3) → P4 Label(4)
  → P5 Leakage/Data Quality(5) → 【真实数据统计 gate：重新评估 T1/T2
  可训练性，允许延期】 → P6 Baseline(6) → P7 ML Training(7)
  → P8 Evaluation(8) → P9 Explainability/Calibration(9) → P10 Model Versioning(10)
```

辅助线（不阻塞主线）：Phase 0 已完成；Phase 11（4.0 接口 schema）全程
独立并行；Phase 12（Offline Prediction）在 P9/P10 后；Phase 13 收尾。
Phase 5 内的可选靶场/replay 增强与 P6-P8 并行，**不阻塞任何基础步骤**。

- **可并行**：P3 ∥ P4；Phase 11 与一切并行；可选数据增强与 P6-P8 并行。
- **必须等待 4.0**：L6、攻击链特征、T6；L5 启用前还差靶场 exploit 样本
  积累（不依赖 4.0，但依赖可选增强）。
- **现在可开始**：Phase 1-10、12（全离线）。
- **暂时不能开始**：T1/T2 的最终训练（等 Phase 5 gate）；4.0 数据接入
  适配器；任何在线/生产集成。
- **不应该做**：gate 未过仍强行训练、深度学习起步、为凑模型数量加模型、
  用 Severity 当 Label、没有 execution 证据造 negative、把 ML 写进
  Go 引擎或前端、伪造任何指标、提前实现 4.0 攻击链字段。

## 8. Commit 规范

每 Phase 独立 commit，前缀 `ml:`，例：
`ml: add dataset schema`、`ml: add feature pipeline`、
`ml: add label pipeline`、`ml: add baseline evaluation`、
`ml: add training pipeline`。数据本体（data/ml/**）不入仓；
本计划/审计报告/TODO 文档修订在 Phase 0 修订提交中入库。
