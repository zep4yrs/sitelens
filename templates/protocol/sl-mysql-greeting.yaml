id: sl-mysql-greeting
info:
  name: MySQL Service Greeting
  severity: low
  tags: network,mysql,sl-curated
tcp:
  - port: 3306
    matchers:
      - type: dsl
        dsl:
          - 'contains(response, "mysql_native_password") || contains(response, "caching_sha2_password")'
