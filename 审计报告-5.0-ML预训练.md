# SiteLens 5.0 ML 预训练代码审计报告

> 审计时间：2026-09-13。分支：`feature/ml-5.0-pretraining`（自 main@70a6f11 切出）。
> 审计方式：从真实程序入口（`cmd/sitelens/main.go`）开始逐包通读全部非 vendor Go 源码
> （31 个 internal 包 + cmd，共 25,476 行 / 915 个文件含 vendor，70 个测试文件），
> 并对 `data/` 全部真实数据文件做了逐项结构与规模统计（本报告所有数字均为
> 2026-09-13 本机实测，非文档转抄）。文档（README/CHANGELOG/开发文档）仅作线索，
> 一切结论以代码与数据为准；文档与代码不符处已单独标注。

---

## 1. 审计范围与方法

已通读（逐文件）：

| 范围 | 内容 | 结论来源 |
|---|---|---|
| 入口 | `cmd/sitelens/main.go`（177 行）、`cmd/sitelens/migrate_pg.go`（147 行） | 全文 |
| 编排 | `internal/engine`（engine.go 777 / result.go / exploit.go / netproto.go） | 全文 |
| 发现产生 | `internal/checks`（checks.go + runner.go）、`internal/dast`（dast.go + blind.go + deser.go + ssrf.go）、`internal/exploit`、`internal/passive`、`internal/nuclei`（index/select/convert/netrun/afrog/sync）、`internal/modules`（dir/subdomain/takeover/webshell/activefp/serviceprobe）、`internal/netsec`、`internal/jsmap`、`internal/loginbrute`、`internal/replay` | 关键结构全文 + 函数签名核点 |
| 情报 | `internal/intel`（intel.go/nvd.go/tplintel.go/cvss.go/kev.go/osv*.go/overrides.go/stats.go/techcpe.go） | 全文 |
| 存储 | `internal/store/store.go`（421 行）、`internal/server`（server.go 1238 + report.go + settings.go + reload.go + spa_target.go） | 全文 |
| 指纹 | `internal/sitelens`（fingerprint.go/evidence.go/matcher.go/favicon.go/litindex.go） | 全文 |
| 支撑 | `internal/config`、`internal/target`、`internal/authn`、`internal/beacon`、`internal/httpx`、`internal/htmlx`、`internal/crawler`、`internal/headless`、`internal/netx`、`internal/dsl`、`internal/rex`、`internal/fpmerge`、`internal/versioncmp`、`internal/captcha`、`internal/audit`（audit.go + taint.go） | 结构与关键函数 |
| 外围 | `web/`（7 页面 + embed.go）、`desktop/`（Electron 壳，数据落 userData）、`installer/`（NSIS）、`tools/`、`Dockerfile`、`go.mod` | 布局与数据落位 |
| 文档 | README.md、CHANGELOG.md、docs/开发文档.md、docs/开发文档-4.0.md、docs/todo-3.0.md、docs/upgrade-3.0.md、docs/产品说明.md | 线索核对 |
| 真实数据 | `data/state/history.json`（119 条扫描）、`history.json.premigrate`（47 条 Python 遗产）、`data/intel_dump.json.gz`、`data/tpl_intel.json.gz`、`data/nvd_cves.json.gz`、`data/intel_overrides.json`、`data/affected_ranges.json`、`data/go/`、`data/state/nuclei_index.json`、靶场矩阵产物 `D:\fengqiao\ranges-lab\tmp_matrix_out\` | 逐文件用脚本统计 |

明确跳过（已确认性质）：`vendor/`（Go 依赖镜像，go mod vendor 产物）、
`desktop/node_modules/`（npm 依赖）、`data/nuclei/`（719MB 模板池，git 忽略，
update-nuclei 可再生）、`bin/ build/ dist/ SiteLens packs`（构建产物）、
`data/wordlists/`（字典）、`data/asset-extras/`（静态资料 JSON）。

---

## 2. 当前 SiteLens 总体架构

技术栈：Go 1.26 单二进制引擎（直接依赖仅 3 个：chromedp / pgx.v5 / yaml.v3，
见 `go.mod`）+ 嵌入式 Web 前端（`web/embed.go` go:embed）+ Electron 桌面壳
（`desktop/`，引擎随包整体升级，数据落 `%APPDATA%` userData）。
文件式存储、无外部数据库（Python 时代的 PG 已由 `migrate-pg` 一次性迁走）。

程序入口（`cmd/sitelens/main.go:67-126` 的 switch）：

| 子命令 | 作用 | 数据后果 |
|---|---|---|
| `scan <url>` | 全流水线扫描，结果 JSON 打 stdout | **不落历史库** |
| `serve` | 内置 Web 服务（默认 127.0.0.1:5000），桌面版的生产入口 | 扫描落 `data/state/history.json` |
| `audit <dir>` | 本地源码静态审计（SAST-lite） | 仅 stdout，不落库 |
| `migrate-pg` | Python 时代 PG `scans` 表一次性迁入文件库 | `store.Import` 幂等并入 |
| `regression <scan_id>` | 重放历史扫描的 dast 类发现 | 仅退出码+输出，不落库 |
| `update-nuclei/afrog/fp/ehole/osv/nvd/tplintel` | 数据包镜像与合并 | 落 `data/` 各文件 |

internal 包清单（31 个）与职责一句话：
`engine`（编排）、`store`（历史+作业）、`server`（REST+页面）、`checks`（验证型
check）、`nuclei`（模板索引/转换/协议运行）、`dast`（参数级注入）、`exploit`
（利用级无害验证）、`replay`（回归重放）、`sitelens`（证据抽取+指纹匹配）、
`fpmerge`（指纹库合并）、`intel`（漏洞情报三级判定+NVD/KEV/OSV/模板情报）、
`modules`（目录/子域/接管/WebShell/主动指纹/端口服务）、`netsec`（TLS/DNS）、
`netx`（TCP/TLS/UDP/DNS 原始拨号）、`loginbrute`（登录爆破）、`captcha`（ddddocr
sidecar 对接）、`authn`（登录态）、`beacon`（SSRF 出带）、`passive`（被动检查）、
`jsmap`（JS 攻击面）、`crawler`+`htmlx`+`httpx`+`headless`（采集层）、`target`
（目标校验+SSRF 防护+gov.cn 硬保护）、`dsl`（安全子集表达式求值器）、`rex`
（受限回溯正则，服务指纹兜底）、`versioncmp`（版本比较/抽取）、`config`、
`security`（响应头评分）、`audit`（SAST-lite）。

三条交付形态（README/CHANGELOG 与 desktop/ 代码核对一致）：
`sitelens serve`（CLI 服务）、桌面安装包（desktop 壳拉起引擎，端口 5087）、
Docker（`Dockerfile`）。

---

## 3. 当前真实数据流

生产数据流（`serve` 模式，桌面版同路径）：

```
POST /api/scan (server.go:438 hScan)
  → scanOptions(body)（server.go:472，level 预设 levelPresets:460-468 七档）
  → store.JobManager.Create（内存作业，200 上限）
  → go runScanJob（server.go:536）
      → engine.Engine.Scan（engine.go:117）
      → store.Store.Save（store.go:88）→ ScanRecord 写入 data/state/history.json
  → GET /api/job/{id}（轮询进度+事件环）/ GET /api/job/{id}/results
  → GET /api/history/{id}（ScanRecord 全文）/ GET /api/export/{id}（json/csv/wide/html/md）
```

Engine.Scan 十一段流水线（engine.go:117-582，行号为真实代码位置）：

| 段 | 代码位置 | 模块 | 产出（进 Result 的位置） |
|---|---|---|---|
| 1 校验 | engine.go:137 | `target.Validate`（SSRF 三层防护+gov.cn 硬闸） | Result.URL/Host/Error |
| 1.5 认证 | engine.go:161-173, 258-277 | `authn.Login`/`authn.AutoDetect` | 无字段，Cookie 进 client（只进事件流） |
| 2 采集首页 | engine.go:176-200 | `httpx`+`htmlx.Parse` | Result.Title/Status/IP/ResponseTimeMS/Pages[0] |
| 3 JS 攻击面+爬取 | engine.go:210-299 | `jsmap.Run`、`crawler.Crawl`（可 headless 渲染） | Result.Pages、Extras["js"]、dastLinks/dastFormTargets（内部） |
| 4 指纹 | engine.go:301-314 | `sitelens.Extract`+`Matcher.Match`（含 favicon mmh3） | Result.Technologies（techAcc 聚合，result.go:50-97） |
| 5 响应头评分 | engine.go:317-318 | `security.Assess`（8 头加权 100 分） | Result.Security |
| 6 check | engine.go:320-354 | `checks.RunChecks`（软 404 基线/回显剔除/二次确认/版本抽取回灌） | Result.Verified（src=check），版本回填 Tech.Version |
| 6.5 Nuclei | engine.go:356-378 | `nuclei.Index/Select`（LRU 轮转）→ `checks.RunList` | Result.Verified（src=nuclei） |
| 7 被动 | engine.go:380-392 | `passive.Run`（Cookie 属性/CSRF 表单） | Result.Verified（src=passive） |
| 8 DAST+利用级 | engine.go:394-450 | `dast.RunForms/Run`（xss/sqli-error/sqli-blind-time/open-redirect/lfi-passwd/deser/ssrf-oob）→ `engine.runExploit`（engine/exploit.go） | Result.Verified（src=dast，含 param/payload/replay）；impact/impact_evidence 回填；Extras["exploit"] 计数 |
| 9 主动模块 | engine.go:452-553 | subdomain/takeover/dir/webshell/active_fp/serviceprobe/netproto/弱口令 | Extras 各键；弱口令命中进 Verified（src=weak） |
| 10 网络层 | engine.go:555-567 | `netsec.CheckTLS/CheckDNSMail` | Extras["netsec"] |
| 11 情报关联 | engine.go:569-576 | `intel.KB.Match`（三级判定）+`MatchCVEMs`+`attachTemplates` | Result.Vulnerabilities |

持久化边界（对 ML 至关重要，"落库 vs 不落库"）：

| 数据 | 产生位置 | 是否持久化 | 落点 |
|---|---|---|---|
| ScanRecord（摘要+options+Result） | `store.Save` store.go:88 | 是 | `data/state/history.json`（原子写，超 `max_records`=500 裁最旧并追加 `history.archive.jsonl`，store.go:110-118） |
| 扫描 options | `runScanJob` server.go:558-561 | 是，**但只存 5 个键**（deep/checks/netsec/dast/passive） | history.json 内 |
| 批量扫描 | server.go:685 | 是（options 只存 `{"batch":true}`） | 同上 |
| CLI `scan` | main.go:159-161 | **否**（仅 stdout） | 无 |
| 实时事件环 JobEvent | `store.AppendEvent` store.go:391 | **否**（内存环形 80 条，作业淘汰即消失） | 无 |
| 利用级验证明细 impact | engine/exploit.go:76-79 | 是（回填 verified map） | history.json 内 |
| Extras["exploit"] 汇总 | engine.go:446-448 | 是 | history.json 内 |
| 回归重放结果 | `replay.Batch` → `/api/replay/{id}` server.go:744-758、CLI regression | **否**（响应即焚） | 无 |
| 登录爆破结果 | server.go:985-990 Job.Result | **否**（作业内存） | 无 |
| 源码审计结果 | `/api/audit` server.go:995-1075、CLI audit | **否**（响应即焚） | 无 |
| KEV 缓存 | `SaveKEVExtra` server.go:271-285 | 是 | `data/state/kev_extra.json` |
| Nuclei 索引/调度表 | `nuclei.IndexCached`、engine.go:722-746 | 是 | `data/state/nuclei_index.json`（35MB）、`nuclei_schedule.json`（14.5MB） |

---

## 4. 3.0 数据流（已发布能力，Version 常量 3.0.0，server.go:46）

3.0 的三大主项（CHANGELOG v3.0.0 与代码核对一致）：

1. **非 HTTP 协议检测**：`internal/netx` 拨号 → `modules.ServiceProbe`
   （11,966 条服务 banner 指纹，RE2 不兼容形态由 `internal/rex` 受限回溯兜底）
   → 协议模板（tcp/dns/ssl）经 `nuclei` 漏斗转 NetCheck → 命中落
   Extras["netproto"]（engine.go:518-523）。当前历史数据中该键 **0 条**
   （协议扫描仅在 full 档+端口识别开启时产生）。
2. **利用级无害验证**：`internal/exploit` 对 DAST 发现做影响证明，
   判定三值 none/observed/proven（exploit.go:28-34），六通道（xss-reflect/
   sqli-error/sqli-blind-time/open-redirect/lfi-passwd/ssrf-oob，
   exploit.go:86-106）。命中回填 verified 的 impact/impact_evidence 键
   （engine/exploit.go:76-79），汇总进 Extras["exploit"]。双闸：请求开关
   opts.Exploit × config exploit.enabled（默认关，config.go:229）。
   注意：CHANGELOG 写的"授权三重闸"已过时——2026-09-13 前置变更移除了
   授权白名单（Gate 只剩 Enabled 一层，exploit.go:38-45；docs/开发文档-4.0.md
   附录六与此一致，以代码为准）。
3. **验证回归**：`internal/replay` 按原 payload 单请求重放，present/gone/
   unsupported 三分（replay.go:57-63），unsupported 诚实不进分母
   （replay.go:73-80）。`regression` CLI + `/api/replay/{id}`。
   重放结果当前**不落库**。

3.0 数据扩容后的静态数据包规模（docs/开发文档.md §5 与 data/ 实测一致）：
模板池 117,889 可运行（data/nuclei/ 719MB，git 忽略可再生）、Web 指纹 13,727
（data/go/technologies.json 3.2MB）、服务指纹 11,966、精选情报 1,985
（intel_dump vuln_kb）、模板情报行 29,554（tpl_intel.json.gz）、
NVD 371,755（nvd_cves.json.gz 37MB）、微软公告 34,931、KEV 1,695
（快照数；开发文档记 1,709 为 24h 更新后的数）、OSV 覆盖 912 键
（intel_overrides.json）。

---

## 5. 当前 4.0 状态判定（已实现 / 开发中 / 仅设计 / 不存在）

`docs/开发文档-4.0.md`（2026-09-13 立项）明确：4.0 第一主线是
**资源占用治理**（引擎空闲 RSS 1GB→300MB 等量化验收），并且原文写明
"后续主线（攻击链视角等）另行开篇"。结合全库代码 grep（cwe/攻击链/AST/
提权 均无生产代码命中，唯一 "CWE-77" 出现在 `internal/nuclei/meta_test.go`
的模板测试夹具里），逐项判定如下：

| 4.0 概念 | 状态 | 证据 |
|---|---|---|
| Black-box Analysis | **已实现**（3.0 语义全覆盖：checks/DAST/Nuclei/主动模块/协议） | engine.go 全文 |
| White-box Analysis | **部分实现**：`internal/audit` SAST-lite（16 条规则，audit.go:35-73）+ TAINT-lite 行级污点（taint.go，仅 .py 源码，source/sink 各一组正则），CLI+`/api/audit` 可用，**结果不落库** | audit.go:7 注释自述"污点分析模块暂未迁移" |
| AST Code Analysis | **不存在**。`internal/dsl` 有自己的表达式 AST（dsl.go:68），但那是模板判定表达式求值器，不是源码 AST 分析 | dsl.go |
| Data Flow | **不存在**（真数据流分析）。taint.go 是行级近似，自述"无函数作用域/流敏感分析" | taint.go:11-14 |
| CWE Vulnerability Relationship | **不存在**（数据结构无 CWE 字段） | 全库 grep |
| Entry Point | **不存在**（最接近物：`jsmap.Endpoint`{endpoint,kind=api|sourcemap}、`PageInfo`，均为采集物而非分析结论） | jsmap.go:31-35 |
| Attack Chain / Chain Node / Chain Edge | **不存在**（4.0 后续主线的立项意向，仅 docs/开发文档-4.0.md 一句话，无文档无代码） | 开发文档-4.0.md:4-5 |
| Privilege Change | **不存在**（最接近物：`loginbrute.Hit` 认证成功事件、exploit verdict，均非权限状态机） | loginbrute.go:151-160 |
| Impact | **部分实现**：verified map 的 impact/impact_evidence 键（3.0 exploit 层）+ `dast.Finding.Impact`（dast.go:421-422，注释"2.0 证据链扩展位"，恒空）。当前 119 条历史中 impact 行数 **0**（全部扫描未开利用级总闸） | engine/exploit.go:76-79 |
| Evidence Correlation | **跨发现关联不存在**；单发现证据链已实现（request/response/signals/replay/confirmed，runner.go:27-42） | runner.go |
| Black-box Evidence | **已实现**（155/456 条 verified 带完整证据链） | 数据实测 |
| White-box Evidence | **部分实现且不落库**：`audit.Finding`{rule,severity,file,line,snippet,match,advice}（audit.go:89-98）只在响应/ stdout 存在 | server.go:1065-1074 |

已落地的 4.0 前置变更（非资源项）：exploit 授权白名单移除（exploit.go:36-45）、
gov.cn 代码级硬保护（`internal/target`，HTTP+netx 双闸）、"仅限授权目标"
文案清理。资源治理 Quick Wins（GC 软上限/NVD 瘦身/惰性加载/FreeOSMemory）
处于"方案已写、代码未动"状态——internal/intel 仍是全量解码常驻
（nvd.go 全量 Unmarshal，server.go:125-127 启动即 AttachNVD）。

**结论：4.0 的黑白盒/攻击链主线尚未开工。5.0 凡依赖 Attack Chain、CWE 关系、
Entry Point、Privilege Change、White-box 持久证据的任务，一律标记"等待 4.0"。**

---

## 6. 所有潜在 ML 数据来源（逐项：产生位置 / 结构 / 存储 / ML 可用性）

按用户给定的数据清单逐项核对。结构字段均为 Go 源码原文。

### 6.1 Asset / Host / IP / Port / Protocol / Service

| 数据 | 产生位置 | 真实结构 | 存储 | ML 可用性 |
|---|---|---|---|---|
| Asset（目标站点） | engine.go:137-147 | Result.URL/Host + ScanRecord 摘要 | history.json | 直接可用（隔离键） |
| Host | target.Validate → res.Host | string | 同上 | 直接可用 |
| IP | engine.go:195 `lookupIP` | Result.IP（单值，v4 优先取第一个） | 同上 | 可用，稀疏（内网靶场多为 127.0.0.1） |
| Port | engine.go:143-146 拼进 baseURL | **无一等字段**，只能从 URL 解析 | 同上 | 可用（解析器需自写） |
| Protocol | 同上（scheme）+ ServiceHit/netproto | scheme 字符串；协议模板命中 Extras["netproto"] | 同上 | scheme 可用；netproto 当前 0 条 |
| Service | engine.go:510-513 `modules.ServiceProbe` | `modules.ServiceHit`{port,service,product,version,banner}（serviceprobe.go:45-51） | Extras["service"] | 可用；当前仅 10 行（34 次扫描开了键、大多空） |

### 6.2 Endpoint / URL / Parameter

| 数据 | 产生位置 | 真实结构 | 存储 | ML 可用性 |
|---|---|---|---|---|
| Endpoint（页面） | crawler → engine.go:245 | `PageInfo`{url,status}（result.go:24-27） | Result.Pages | 可用（0-10 行/次，中位 2，浅爬上限 max_pages=4 所致） |
| Endpoint（JS 攻击面） | engine.go:212-226 `jsmap.Run` | `jsmap.Endpoint`{endpoint,kind} | Extras["js"] | 可用；当前 16 行 |
| URL（发现级） | 各发现结构自带 url 键 | verified map["url"] | Verified | 可用 |
| Parameter | dast.go:484-488 `collectParams`（主机+路径+参数名去重） | `dast.Finding.Param`；verified map["param"] | Verified（仅 9 行有 param） | 可用但极稀疏 |

### 6.3 Technology / Version / Fingerprint

| 数据 | 产生位置 | 真实结构 | 存储 | ML 可用性 |
|---|---|---|---|---|
| Technology | engine.go:301-314，`techAcc.add` 聚合（result.go:59-88：同名合并、证据累加、置信+5/次、封顶 100） | `engine.Tech`{name,categories,confidence,version,website,evidence}（result.go:14-21） | Result.Technologies | **核心特征源**。636 行/115 唯一/仅 5 个带版本 |
| Version | 指纹正则捕获组（fingerprint.go:289-294，首捕获组以数字开头才采信）+ check 抽取回灌（result.go:111-121，仅 wp-readme→WordPress 一条链路 checks.go:146-148） | Tech.Version | 同上 | 版本覆盖率 4.2%，是情报 confirmed 率低的直接原因 |
| Fingerprint（证据） | `sitelens.Evidence`{url,status,headers,body,title,scriptSrcs,metas,cookieNames,faviconHash}（evidence.go:13-24）；命中证据是 Tech.Evidence 字符串数组（"header: X=…" / "meta …" / "正则 …" 形态） | 字符串 | 同上 | 可做"证据通道类型"特征；原始 body **不落库**（每次扫描的页面正文只在内存，未持久化） |
| favicon hash | `sitelens.FaviconHashOf`（favicon.go） | mmh3 int64，只进 Evidence 参与匹配，**不落库** | 无 | 当前不可用（历史无此字段） |

### 6.4 Vulnerability / CVE / CWE / CVSS / Severity

| 数据 | 产生位置 | 真实结构 | 存储 | ML 可用性 |
|---|---|---|---|---|
| Vulnerability（情报） | engine.go:570-576：`KB.Match`（intel.go:270-341，三级判定 confirmed/possible/excluded）+`MatchCVEMs`（intel.go:400-442）+`attachTemplates`（engine.go:656-678，CVE→模板路径，单 CVE 上限 6 条） | `intel.Finding`{id,tech,version,src,name,title,product,cve,type,severity,severity_zh,cvss_score,cvss_sev,ref,desc,verdict,affected,kev,templates}（intel.go:66-86） | Result.Vulnerabilities | **核心数据源**。1,135 行/77 唯一 CVE |
| CVE | Finding.CVE | string | 同上 | 可用 |
| CWE | **不存在**（全库无生产字段；NVD 原始数据里有，但 `NVDEntry` 投影未保留 CWE 字段，nvd.go:31-40） | — | — | 等待：若 5.0 需 CWE，可在 nvd 同步管线加投影（改 update-nvd 属数据包工程，非扫描行为） |
| CVSS | vuln_kb 富集列 + NVD 补全（`nvdFill`）+ OSV 覆盖（overrides.go） | Finding.CVSSScore/CVSSSev/CVSSVectorTxt（float64/string） | 同上 | 可用；历史仅 88/1135 行有分 |
| Severity | v.Severity 原样透传 | string | 同上 | **格式脏**：'important'/'Important'/'Critical'（微软公告大小写混排）与四档并存，见 §9 |

### 6.5 Intelligence / NVD / KEV / Template / Template Intelligence

| 数据 | 产生位置 | 真实结构 | 存储 | ML 可用性 |
|---|---|---|---|---|
| Intelligence（关联结论） | 见上 | intel.Finding | history.json | 可用 |
| Intelligence（知识库本体） | `intel.Load`（intel.go:151-208）：intel_dump.json.gz 的 tables{vuln_kb,cve_ms,kev,tscan_fingerprints,fingerdir,service_fp} | `Entry`/`CVEMsRow`/`KevEntry`/`FingerDirRow`/`ServiceFPRow` | data/intel_dump.json.gz（git 跟踪） | 静态字典特征源（可版本化） |
| NVD | `intel.LoadNVD`/`AttachNVD`（nvd.go） | `NVDEntry`{cve,pub,mod,sev,score,vector,descr,prods[]}（CPE 约束 `NVDProd`） | data/nvd_cves.json.gz | **高价值静态特征源**（发布时间/评分/CPE/受影响区间投影） |
| KEV | intel_dump tables.kev + 守护协程 `FetchKEV`→`MergeKEV`（server.go:267-294） | `KevEntry`{cve,date_added,ransomware}；Finding.KEV bool | data/state/kev_extra.json + Finding | 可用（在野利用是最强先验之一；历史 35 行 kev=true） |
| Template | `nuclei.Index/Select`（nuclei.go:60,220）：索引 Entry{path,name,tags,sev,cves,proto,mtime,size,convertible,lastrun}；LRU 轮转表落盘 | `nuclei.Entry`；调度表 map[path]lastRun | data/state/nuclei_index.json（35MB）、nuclei_schedule.json（14.5MB） | 可用（模板元数据=验证先验；lastrun 反映调度历史） |
| Template Intelligence | `update-tplintel` 产物 `LoadTplIntel`/`AttachTplIntel`（tplintel.go）：模板 CVE × NVD 关联行，负 ID 段并入主索引 | intel.Entry 形态（src='tpl'） | data/tpl_intel.json.gz | 可用（29,554 行：模板覆盖率特征） |
| 精选区间 | `CuratedRange`（intel.go:43-47），data/affected_ranges.json（9 项技术） | {cve,affected,title} | 同文件 | 可用 |

### 6.6 Verification / Evidence / Request / Response / OOB / Timing / Error

| 数据 | 产生位置 | 真实结构 | 存储 | ML 可用性 |
|---|---|---|---|---|
| Verification | checks（src=check）、nuclei（src=nuclei）、passive（src=passive）、dast（src=dast）、js（src=js）、weak（src=weak）六路统一进 `verifiedMap`（result.go:124-129） | 异构 `map[string]any`，统一键 check/title/severity/url/evidence/advice/src（+check 路的 request/replay/response/signals/confirmed/version；dast 路的 param/payload/replay） | Result.Verified | **核心数据源**。456 行 |
| Verification 结果（二次确认） | runner.go:184-213 组级独立重放确认 | `Hit.Confirmed` bool | verified["confirmed"] | 可用（155 行 true） |
| Evidence | `Hit.Evidence` 文本 + `Signals` 通道明细 | string / []string | 同上 | 可做文本特征（中文句子，需特征化处理） |
| Request | `requestText`（runner.go:417-442）：方法/路径/Host/CT/Length 报文文本；运行时头（UA/Cookie）刻意不落证据（防会话泄漏） | string | verified["request"] | 可用（155 行） |
| Response | `HitResponse`{status,size,snippet}（runner.go:19-23）——**快照仅 240 字符**，全量响应靠 replay 命令复现 | object | verified["response"] | status/size 可用；snippet 可做轻量文本特征 |
| OOB | `beacon.Reserve/Hit`（beacon.go）+ exploit 出带通道（engine/exploit.go:30-59）；命中即 proven | verdict 进 impact 字段；beacon 回连本身只在事件流 | impact（0 行）/事件环（不落库） | 历史不可用；未来开闸后可用 |
| Timing | `dast.timeBlind` 基线/注入/复测三计时进 Signals 字符串（dast.go:360-376："基线 Xms / 注入 Yms / 复测 Zms"）；Result.ResponseTimeMS（首页 RTT） | 字符串内嵌数字 / int | Verified / Result | 可用但需正则抽取；粒度粗 |
| Error | dast judge 的 evidence、exploit observed 通道（DBMS 指纹提取 exploit.go:126-142）、Result.Error | 文本 | 同上 | 可用（非结构化） |

### 6.7 Authentication State / Exploit / Validation / Regression

| 数据 | 产生位置 | 真实结构 | 存储 | ML 可用性 |
|---|---|---|---|---|
| Authentication State | `authn.Login/AutoDetect`（authn.go）；成功仅进事件流（engine.go:163-172） | 无结构化字段（Cookie 进 client 不落证据） | 事件环（不落库） | **历史不可用**。趋势特征只能在未来显式落库后才有 |
| Exploit | `exploit.Prover.Prove`（exploit.go:86-106） | `Verdict` none/observed/proven + evidence 文本 → verified["impact"]/["impact_evidence"]、Extras["exploit"]{proven,observed,gate} | history.json（本批历史全为 0——119 次扫描全未开总闸） | 结构可用、样本为零 |
| Exploit Validation | 同上 | impact 键 | 同上 | 同上 |
| Validation Result | checks 二次确认 `Confirmed`；`replay.One/Batch`（replay.go:89-154） | confirmed bool；present/gone/unsupported | confirmed 落库；**重放结果不落库** | confirmed 可用；回归标签需先建存储再积累 |
| Regression | `regression` CLI + `/api/replay/{id}` | `replay.Stats`/`Result` | **不落库** | 同上 |
| 历史扫描 / 扫描结果 | `store.Store`（store.go 全文） | `ScanRecord`{ScanSummary{id,url,host,title,status,security_grade,security_score,tech_count,vuln_count,duration,scanned_at}, options, result}；`history`{next_id,scans[]}（store.go:24-50） | data/state/history.json + history.archive.jsonl（当前 0 行）+ premigrate 快照 | **主数据源**。119 条 |

---

## 7. 当前历史数据规模（2026-09-13 实测）

来源：`data/state/history.json`（119 条，next_id=120；含 47 条 migrate-pg 迁入的
Python 时代记录，id≤47）、`history.json.premigrate`（47 条迁移前快照）、
靶场矩阵产物 4 个 ScanRecord JSON（`D:\fengqiao\ranges-lab\tmp_matrix_out\`，
与 store 同构、可经 Import 回灌）。

| 维度 | 数字 |
|---|---|
| 扫描记录 | 119（无归档行；时间跨度 2026-09-05 01:23 → 09-12 15:34） |
| 唯一 Host | 10（lingyun.feng-qiao.top 41 次 / 127.0.0.1 21 / tools.feng-qiao.top 20 / www.text-well.com 18 / feng-qiao.top 7 / books.toscrape.com 6 / 其余 ≤2） |
| 扫描时长 | 0.115s – 2362.8s，中位 8.38s |
| 每次扫描页面数 | 0–10，中位 2（crawler.max_pages=4 所限） |
| 技术行 | 636 行 / 115 唯一技术 / 仅 5 个技术出现过版本号（Top：Nginx 62、PHP 55、jQuery 51、Google Font API 23、Apache 20） |
| 情报发现 | 1,135 行 / 77 唯一 CVE；verdict：possible 954、confirmed 12、空 169（全部来自 premigrate 旧 schema）；带 CVSS 88 行、KEV 红标 35 行、挂模板 53 行；src：tscan 648 / cve_ms 285 / afrog 127 / xray 19 / tpl 53 / ms-bulletin 3 |
| Verified | 456 行：src passive 205 / nuclei 216 / check 26 / dast 9；带完整证据链（request/replay/response/signals/confirmed）155 行；Top check：cookie-attrs 189（41%）、csrf-form 16、nuclei-php-detect 16、nuclei-apache-detect 15、admin-path 14、nuclei-header-sqli 12 |
| 利用级 impact | **0 行**（Extras["exploit"] 从未出现：全部扫描未开 exploit.enabled） |
| Extras 行 | dir 1,560（36 次扫描）/ webshell 148 / netsec findings 118 / js 端点 16 / subdomain 12 / service 10 / active_fp 0 行（34 次开键空）/ takeover 3 次开键 / netproto 0 |
| 安全评分 | F 83 次 / A+ 26 / A 2 |
| 静态字典 | 指纹 13,727；服务指纹 11,966；vuln_kb 1,985；tpl_intel 29,554（10,289 产品）；NVD 371,755；cve_ms 34,931；KEV 1,695；OSV 覆盖 912；模板索引缓存 35MB |
| 靶场矩阵 | 4 份 ScanRecord（dvwa_apoc/dvwa_full/sqli_full/xss_full），119 条历史之外的可回灌增量 |

---

## 8. 真实数据结构（供 Dataset Schema 直接对齐的 Go 定义）

```
engine.Result        (result.go:32-48)
  url/host/title/status/ip/response_time_ms/scanned_at/duration/error/
  security{score,grade,items[8]} / technologies[]Tech /
  vulnerabilities[]intel.Finding / verified[]map / extras map / pages[]PageInfo

engine.Tech          (result.go:14-21)
  name/categories[]/confidence(0-100)/version/website/evidence[]string

intel.Finding        (intel.go:66-86)
  id/tech/version/src/name/title/product/cve/type/severity/severity_zh/
  cvss_score/cvss_sev/ref/desc/verdict(confirmed|possible|"")/affected/
  kev(bool)/templates[]string

verified map 条目（异构，verifiedMap 统一 src 键）
  check/title/severity/url/evidence/advice/src
  check 路+: version/request/response{status,size,snippet}/signals[]/
             replay/confirmed
  dast 路 +: param/payload/replay/request/response/signals
  exploit 命中+: impact(none|observed|proven)/impact_evidence

store.ScanRecord     (store.go:40-44)
  ScanSummary{id,url,host,title,status,security_grade,security_score,
              tech_count,vuln_count,duration,scanned_at}
  + options map + result *engine.Result

modules.ServiceHit   (serviceprobe.go:45-51)  port/service/product/version/banner
modules.PageHit      (modules.go:25-32)       url/path/status/size/title/bypass
netsec.Finding       (netsec.go:18-25)        check/title/severity/url/evidence/advice
jsmap.Endpoint       (jsmap.go:31-34)         endpoint/kind(api|sourcemap)
modules.TakeoverHit  (takeover.go:171-177)    sub/service/severity/evidence/advice
audit.Finding        (audit.go:89-98)         rule/severity/title/file/line/snippet/match/advice —— 不落库
```

---

## 9. 数据质量问题

1. **重复数据**：119 次扫描里 10 个 host 被反复扫（同一 check+url 的 verified
   行，58/140 个去重键出现 >1 次；lfi-passwd@127.0.0.1:8090 单键重复 6 次）。
   重复不是脏数据——是"重扫时间序列"，但对随机划分是致命泄漏源（§10）。
2. **类别不平衡**（极重）：verified 里 passive 配置审查类占 45%
   （cookie-attrs 189/456）；安全评分 F 占 70%（83/111 有评分）；dast 命中
   仅 9 行；exploit 正样本 **0**；takeover/netproto 命中 0。任何"命中/利用"
   二分类在当前历史上都接近零正样本学习。
3. **稀疏**：版本覆盖率 4.2%（636 行技术行仅 27 行带版本）→ 情报三级判定里
   confirmed 仅 12 行（1.1%）、95.4% 是 possible；CVSS 覆盖 7.8%；模板挂载
   4.7%。FaviconHash、原始响应体、页面正文均未落库。
4. **格式脏**：severity 大小写/词表不统一——go 时代 408 行里出现
   'Important' 266 / 'Critical' 19（cve_ms 通道原样透传，engine.go:767-777
   不做归一），premigrate 里另有 'important' 2；verdict 空串 169 行全部来自
   Python 旧 schema（id≤47）。severity_zh 映射表只认小写四档
   （intel.go:92-94），大小写混排会漏翻。
5. **记录口径漂移**：serve 路径只存 options 的 5 个键（server.go:558-561，
   level=apocalypse 与 level=deep 落库后不可区分，除非恰好改了那 5 键）；
   batch 只存 {"batch":true}；CLI scan 完全不落库。**无法从历史重建当时
   的完整扫描配置**——这是 ML 数据完整性的最大结构性缺口。
6. **响应快照有损**：verified.response 只有 status/size/240 字符 snippet
   （runner.go:19-23, 468-477），重放靠 curl 命令文本。Request/Response 特征
   只能做轻量文本/统计特征，做不了深度 NLP。
7. **情报行截断**：单技术最多 20 条情报（intel.go:96 maxPerTech）、单 CVE 最多
   6 条模板（engine.go:665-669）、微软公告每扫描 20 条封顶
   （engine.go:574）——历史中的 vulnerabilities 是**被配额截断的视图**，
   直接当全量标签用会有截断偏差（顶部 severity 排序保证截的是低危，特征上
   要记住这一点）。
8. **靶场偏置**：数据主体是自有靶场/自家站点（127.0.0.1 容器农场、
   *.feng-qiao.top），目录/webshell 命中里含 catch-all 误报倾向
   （webshell 148 行里靶场容器占多数）。domain generalization 能力目前无法
   验证。

## 10. 数据泄漏风险（防泄漏设计输入）

- **组泄漏（最危险）**：同一 host 多次扫描。任何随机 train/test split 都会把
  同一目标的"答案"泄进训练集。必须 Host 分组隔离（GroupShuffle by host），
  Target/Asset 隔离是硬要求而非可选项。
- **时间泄漏**：premigrate（9-05 前）与 go 时代（9-05 后）schema 不同、
  模板池/指纹库版本也不同（3.0 扩容发生在 9-12）。按 scanned_at 切分时，
  数据包版本必须作为 dataset 元数据记录，否则"性能变化"无法归因。
- **特征-标签同源泄漏**：举例——若任务是"预测该 CVE 值不值得进利用级验证"，
  label=impact(proven/observed)，则 verified.impact/impact_evidence、
  Extras["exploit"] 本身绝不能进特征；同理 predicted="confirmed verdict"
  时，verdict/affected（版本区间判定输出）不能进特征。每条特征的
  **Prediction Time 语义**必须在 Feature Pipeline 里显式声明（P0=扫描时点
  已知 / P1=扫描产物 / F=未来结果（禁入））。
- **截断偏差泄漏**：§9.7 的配额截断使"低危情报行"系统性缺席，若任务涉及
  "哪些情报被跳过"，历史数据答不了。
- **评估集污染**：靶场 DVWA/pikachu/sqli-labs 的漏洞位型是公开已知的，
  在靶场上调参再报靶场指标 = 教材答案背书。报告指标必须区分
  farm-heldout 与真实目标 heldout。
- **好消息**：Request/Response 证据里刻意不含运行时头（UA/Cookie，
  runner.go:414-416 注释），历史数据无会话凭证泄漏；gov.cn 硬保护保证
  不会出现敏感目标数据。

## 11. 可以直接复用的数据（零扫描成本）

- 119 条 ScanRecord 全量（history.json + premigrate + 靶场矩阵 4 条）：
  站点级/技术级/情报级/verified 级四层全部字段。
- 静态字典全部（指纹/服务指纹/vuln_kb/tpl_intel/NVD/KEV/cve_ms/OSV 覆盖/
  精选区间/模板索引缓存）：作为**版本化外部特征表**直接挂接，如
  CVE→CVSS 向量、CVE→KEV、product→CPE、模板→CVE 覆盖率。
- nuclei_schedule.json 的 lastrun 表：模板调度历史（哪个模板何时被轮到）。

## 12. 可以通过历史扫描/受控新扫描生成的数据（低成本增量）

- **回归标签**：对 456 行 verified 里 158 行带 payload/replay 的行跑
  replay（或 regression CLI），即得 present/gone 标签——需要先给重放结果
  建存储（当前不落库）。
- **模板验证标签**：对历史里"情报命中 + 有可运行模板"的 (host, CVE) 对，
  在授权靶场重跑 deep/all 档并开 Nuclei 全量，得到"模板真命中"标签——
  现有 53 行挂模板发现只是"有模板可跑"，跑没跑、中没中不在历史里。
- **exploit 正样本**：在自有靶场开 exploit.enabled 重扫（默认关是产品语义，
  ML 侧用显式授权靶场跑批生成训练数据），可积累 proven/observed 标签。
- **options 全量补录**：自下次扫描起，在 ScanRecord.options 里补全键位
  （见 §14 修改建议——需要单独拍板，因为它改 3.0 代码文件）。

## 13. Feature / Label 候选（基于真实字段）

**Feature（P0=扫描时点已知，P1=扫描产物）**
- 资产/网络（P0）：scheme、port、IP 归属（内网/公网）、host 后缀、首页
  status/response_time_ms、页面数、security 8 头逐项 present/value、score。
- 技术栈（P0/P1）：115 维技术存在 one-hot（或类别聚合 33 类）、confidence、
  证据通道类型计数（header/meta/html/src/icon_hash）、版本有无、技术共现对。
- 情报/CVE（P0 静态表联接）：CVE→cvss_score/vector 分解（AV/AC/PR/UI/S/C/I/A）、
  kev、 ransomware、发布距今天数（NVD pub）、产品 CPE 匹配层级
  （tpl_intel 覆盖与否、vuln_kb src 类型 afrog/tscan/xray/cve_ms/tpl）、
  模板可用性（该 CVE 有几条 convertible 模板）。
- 验证面（P1）：check/nuclei 模板级别——历史命中频次（host-leave-one-out
  统计，防泄漏）、模板 tags/sev/proto、check 等级（Lv0/Lv1）。
- 证据强度（P1）：confirmed、signals 条数、response.size、evidence 文本
  统计特征（长度/关键词）、timing 抽取数值。
- 认证状态（P1，当前恒缺）：无。
- 攻击链/白盒（等待 4.0）：无。

**Label（可靠性逐项声明）**
- L1 verified-hit（某 check/模板在某目标是否命中）：可靠（判定即规则定义）。
- L2 模板验证命中（CVE 级）：可靠，需受控新扫描生成（§12）。
- L3 confirmed-verdict（版本落区间）：可靠但接近确定性规则（versioncmp），
  ML 学它无意义，只当 sanity 任务。
- L4 回归 present/gone：可靠，需重放积累。
- L5 exploit proven/observed：语义最贴近"可利用性"，**当前 0 样本，
  暂缓**；且绝不能用 Severity 顶替（用户红线 + 数据上 severity 与 impact
  无必然关系）。
- L6 攻击链/权限变化：不存在，等待 4.0。

## 14. 当前缺失的数据（5.0 需要但 3.0 历史 没有）

1. 完整扫描 options（配额截断与 level 不可复原，§9.5）。
2. 原始响应体/证据全文（只有 240 字符快照）。
3. favicon hash、Evidence 结构化落库。
4. exploit 判定样本（0）、回归重放结果落库、登录爆破/审计结果落库。
5. 认证态结构化记录。
6. CWE 字段（NVD 投影未保留）、EPSS 类外部先验（全库无）。
7. 4.0 全部：攻击链节点/边、入口点、CWE 关系、权限变化、黑白盒证据关联
   （§5，全部标"等待 4.0"）。

## 15. 适合建立 Dataset / Feature / Label Pipeline 的代码位置

- **Dataset 抽取锚点**：`internal/store/store.go` 的 `ScanRecord`/`history`
  是唯一权威读取面——ML 抽取器直接消费 `data/state/history.json` +
  `history.archive.jsonl`（同构 JSON Lines）+ 靶场矩阵 ScanRecord 文件，
  不需要碰 Go 代码；`store.Import`（store.go:137-154）证明外部 ScanRecord
  可无损并入，反过来 ML 侧也应把"生成的数据集"按只读快照对待。
- **Feature Pipeline 锚点**：`engine.Result` 的 JSON 形态即特征抽取输入
  （字段见 §8）；静态表挂接复用 `internal/intel` 的文件路径约定
  （config.go:232-241 IntelConfig）；版本/区间逻辑对齐
  `internal/versioncmp`（特征语义与其一致，避免自造轮子口径漂移）。
- **Label Pipeline 锚点**：verified map 的 src/check/impact 键位
  （engine.go 各回填点 + verifiedMap）；重放标签对齐 `internal/replay`
  的 present/gone/unsupported 语义与"unsupported 不进分母"的诚实口径。
- **评估对齐锚点**：排序任务的"现行规则基线"就是 engine 里
  severityOrder 排序（intel.go:88-90, 337-339）与 nuclei.Select 的
  tags+余弦相关度选模板（nuclei.go:220 起）——ML 的第一职责是击败这两条
  既有规则。

## 16. 当前代码中不能修改的地方（红线）

1. `internal/engine`/`internal/dast`/`internal/checks`/`internal/nuclei`/
   `internal/intel`/`internal/modules`/`internal/netsec`/`internal/exploit`/
   `internal/replay` 等全部扫描行为代码——5.0 预训练阶段只读不写。
2. `internal/store` 的写路径与 history.json 格式——历史库是唯一数据源，
   格式变更会切断 3.0/4.0 的数据连续性（ML 只做读取器）。
3. `internal/target` 的 SSRF 防护与 gov.cn 硬保护——安全闸一行不动。
4. exploit 双闸默认关语义（config.go:229）——ML 生成训练数据时用
   显式配置在靶场开闸，不改默认值。
5. data/state/（git 忽略的本机数据）不进仓；intel_dump.json.gz.orig
   等本地产物不进仓；push 仍需逐次确认（项目惯例）。
6. 前端 web/ 与 desktop/ 壳——本阶段无 UI。
7. CHANGELOG 等历史记录不改写（项目惯例，docs/开发文档-4.0.md 附录三有先例）。

## 17. 未来 5.0 ML 架构建议

原则：**ML 是旁路，不是插件**。Python 侧流水线（本机 python3.13，
scikit-learn 足够，第一阶段禁深度学习）只读 history.json 与静态字典，
产物全部落 `data/ml/`（git 忽略原始数据，manifest/metrics/schema 进仓），
Go 引擎零改动。数据流：

```
data/state/history.json(+archive+premigrate+矩阵 ScanRecord)   静态字典(data/*.json.gz)
        │                                                          │
        ▼                                                          ▼
  raw 层(只读快照+来源指针) ───────────────────────────────► clean 层(去重键/归一 severity/era 标记)
        │                                                          │
        ▼                                                          ▼
  dataset 层(split 元数据: host/time 隔离 + 泄漏检查报告) ◄── feature 层(P0/P1 特征表) ── label 层(L1-L4)
        │
        ▼
  models/experiments/metrics(每训练一次一个 manifest: dataset/feature/label 版本+参数+指标+git rev)
```

任务取舍（按数据现实，能做/暂缓分明）：

| 任务 | 输入→输出 | 现状判定 |
|---|---|---|
| T1 验证目标优先级（模板/check 命中预测，站点×验证面排序） | 站点特征+模板/check 元数据 → 命中概率/排序 | **可做**（L1 有 456 行+受控扫描可扩）；直接对位 nuclei.Select 现行启发式 |
| T2 风险排序（情报发现的价值排序） | CVE/技术/资产特征 → 排序（L2/L1 复合标签） | **可做但标签薄**（confirmed 12 行、kev 35 行），先做规则基线对照 |
| T3 漏洞/结果关系判断（技术栈组合→verified 类别分布） | 站点特征 → check 类别多标签 | **可做**，弱监督定位，仅作 sanity/基线 |
| T4 可利用性预测 | 同上 → proven/observed | **暂缓**（0 样本，等 §12 积累） |
| T5 验证策略推荐（顺序/深度/参数） | 序列决策 | **暂缓**（需 T1/T2 的模型先立住 + 更多扫描序列数据） |
| T6 攻击链/权限/白盒类 | — | **等待 4.0**，接口先行（§19） |

模型路线：Severity/CVSS/KEV/现行排序四条规则基线 → Logistic Regression /
Random Forest / Gradient Boosting（样本量到千级验证面前不引入更多模型族；
不为了模型数量加模型）。解释性输出契约：每条预测给
risk_score / confidence / top_features（特征贡献）/ prediction_reason
（模板化中文短句，引用触发特征值）。

## 18. 当前 5.0 最小可行实现（MVP 定义）

1. `ml/`（Python 包）：读取器（history.json/字典表）→ 清洗（severity 归一、
   era 标记、去重键）→ 特征表（§13 P0/P1 子集）→ 标签表（L1）→
   host 隔离 split → 规则基线 + LR/GBDT → 指标（Ranking P@k/nDCG +
   分类 P/R/F1/AUC）→ manifest 版本化 → 泄漏检查报告。
2. 数据目录：`data/ml/{raw,dataset,features,labels,models,experiments,metrics}`
   全部 git 忽略；schema/manifest 样例与报告进仓。
3. 一个可复现实验：T1 在当前历史 + 靶场矩阵数据上的 host 隔离评估，
   与 Severity/CVSS 基线对照，产出第一份真实指标报告（指标只来自真实
   运行，禁止任何手填数字）。
4. 4.0 数据接口 schema 文档（只定义、不实现），等 4.0 攻击链数据落地后接。

## 19. 4.0 → 5.0 数据接口（先设计，不实现，不修改 4.0）

| 4.0 概念 | 5.0 期待的接口形态（草案） | 状态 |
|---|---|---|
| 漏洞节点 | vuln_node{id, cve, cwe[], tech_ref, evidence_refs[], source(blackbox|whitebox)} | 等 4.0（当前无此结构） |
| 攻击链节点 | chain_node{id, kind(entry|action|privilege|impact), ref(发现/入口点 id)} | 等 4.0 |
| 攻击链边 | chain_edge{src_node, dst_node, relation, evidence_ref} | 等 4.0 |
| 入口点 | entry_point{url|param|endpoint, kind, reachable} | 等 4.0（jsmap.Endpoint 是唯一近亲，语义不同） |
| CWE 关系 | cwe_rel{cve, cwe, chain_ref} | 等 4.0（NVD 投影可先行补 CWE 字段，属数据包工程） |
| Data Flow | dataflow{source, sink, path_ref} | 等 4.0（audit.TAINT-lite 升级后才有） |
| Privilege Change | priv_change{before, after, trigger_ref} | 等 4.0 |
| Impact | impact{kind, evidence_ref, verdict} | 半成品（3.0 impact 键已有语义，0 样本） |
| Evidence 关联 | evidence_link{finding_a, finding_b, relation} | 等 4.0 |
| Black-box/White-box Evidence | 各自沿用现有结构+新增持久化位 | bb 已有；wb 等落库 |

接口原则：5.0 侧先在 `ml/` 里定义只读投影 schema（dataclass/JSON Schema），
4.0 数据一落地即挂接；4.0 未落地前所有相关特征位保持显式"缺失"标记
（不用 0/空串冒充）。

---

## 20. 结论

- 3.0 的历史数据足以支撑 **Dataset/Feature/Label 基础设施 + 验证目标
  优先级（T1）与风险排序（T2）的规则基线对照**；数据规模（119 扫描/10 host/
  456 verified）决定了第一阶段只能做轻量模型与诚实的小样本评估。
- 可利用性（T4）、验证策略（T5）、一切攻击链/白盒能力**暂缓或等待 4.0**，
  不硬做。
- 最重的三个结构性缺口：options 记录不全（§9.5）、exploit 正样本为零
  （§13 L5）、重放/审计结果不落库（§3 持久化边界）。前两者可在"不碰扫描
  行为"前提下用受控靶场扫描 + 一处 additive 落库扩充解决（后者需单独拍板）。
- 本报告全部结论可由 `data/state/history.json` 与所引文件行号复核。
